*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
/.codebuddy

# 《大地巡旅》原文。刻意不入库：体积 1.16 MB，且版权归鹰角网络所有，
# 本仓库只在「出处引用与考据讨论」的范围内收录片段。
# 语料节录由 App\Support\TerraTourCorpus 在 seed 时从本地文件抽取后写入库，
# 文件缺失时该出处语料留空、seed 照常跑完（见 README「引用可定位」）。
/docs/TERRA A JOURNEY.txt
/docs/prts-干员一览.json
/docs/fz-干员一览.json

# 抓取清单。头像本体已随仓库分发（public/assets/avatars/<世界>/，目录按所属世界命名）；
# 清单只在 seed 关联时用（App\Support\CharacterAvatars：按「世界 + 人物名」精确匹配，
# 缺失时 seed 照常跑完，人物卡退回「首字方块」），内含抓取机的本地绝对路径，不入库。
/docs/manifest.json

# 徽记清单与立绘清单**刻意入库**（docs/emblems.json、docs/splashes.json，
# 分别配 public/assets/emblems/ 与 public/assets/splashes/<世界>/）。
# 与上面那份的唯一差别是它们记的是相对 public/ 的路径 —— 没有本机信息，也就没有
# 入库的障碍；而入库换来的是：任何机器上 migrate:fresh --seed 都能把图关联上。
# 上面那份做不到这一点（图在仓库里，路径却写不进库），属于既有的坑，暂未回头改。
#
# 注意这一段**只是说明，没有任何忽略规则**：这两个文件曾经被 /docs/emblems.json
# 一条忽略规则误伤过（注释说入库、规则却在忽略），结果是新克隆的仓库里徽记一个都
# 关联不上，而页面上只会安静地不显示 —— 不要把它们加回忽略列表。
/docs/splashes.json
/docs/emblems.json
